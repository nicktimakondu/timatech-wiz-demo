apiVersion: v1
kind: Service
metadata:
  name: {{ include "timatech.name" . }}-www
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: {{ include "timatech.name" . }}-www
  type: LoadBalancer
